{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Rust lldb",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/rust",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}